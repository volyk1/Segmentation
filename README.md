# Segmentation
Advanced analytics pipeline: RFM preprocessing, K-Means++ clustering, and Deep Learning (TensorFlow) for client classification. Includes Explainable AI (SHAP &amp; LIME) to interpret model decisions. Generates automated marketing strategies &amp; JSON knowledge base for AI agents (n8n).
